function exercise(arr){
    let array=[];
  
  
    for (let i = 0; i < arr.length; i++) {
       if(arr[i] % 2 !== 0){
       
        array.push(arr[i]);
       }
else if( count=array[i] ){
    count % 2 !=0
}

return array;
    }
}
console.log(exercise([1,2,3,4,5]));
